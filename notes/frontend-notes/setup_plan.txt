Production-Level Angular Project Structure
Step 1: Create Angular Project
powershell
# Navigate to project root
cd D:\enterprise-order-management-system

# Create Angular project with strict mode, SCSS, and routing
ng new frontend --routing --style=scss --strict --package-manager=npm

# Navigate to frontend directory
cd frontend
Step 2: Install Production Dependencies
powershell
# Core dependencies
npm install @angular/material @angular/cdk @angular/animations
npm install @angular/flex-layout

# State Management
npm install @ngrx/store @ngrx/effects @ngrx/store-devtools

# HTTP & API
npm install @auth0/angular-jwt
npm install rxjs

# UI Components
npm install ngx-toastr
npm install @ngx-translate/core @ngx-translate/http-loader

# Forms & Validation
npm install @angular/forms @angular/reactive-forms

# Utilities
npm install lodash
npm install moment

# Development dependencies
npm install -D @types/lodash @types/node
Step 3: Create Complete Folder Structure
powershell
# Navigate to app directory
cd src\app

# Create core module structure
mkdir core\auth
mkdir core\auth\components
mkdir core\auth\pages
mkdir core\auth\services
mkdir core\auth\models
mkdir core\auth\guards
mkdir core\auth\interceptors
mkdir core\auth\store

# Create features module structure
mkdir features
mkdir features\products
mkdir features\products\components
mkdir features\products\pages
mkdir features\products\services
mkdir features\products\models
mkdir features\products\store

mkdir features\orders
mkdir features\orders\components
mkdir features\orders\pages
mkdir features\orders\services
mkdir features\orders\models
mkdir features\orders\store

mkdir features\payments
mkdir features\payments\components
mkdir features\payments\pages
mkdir features\payments\services
mkdir features\payments\models

mkdir features\inventory
mkdir features\inventory\components
mkdir features\inventory\pages
mkdir features\inventory\services
mkdir features\inventory\models

mkdir features\dashboard
mkdir features\dashboard\components
mkdir features\dashboard\pages
mkdir features\dashboard\services
mkdir features\dashboard\models

# Create shared module structure
mkdir shared
mkdir shared\components
mkdir shared\components\buttons
mkdir shared\components\cards
mkdir shared\components\modals
mkdir shared\components\tables
mkdir shared\components\forms
mkdir shared\directives
mkdir shared\pipes
mkdir shared\models
mkdir shared\validators
mkdir shared\constants

# Create core services (global)
mkdir core\services
mkdir core\interceptors
mkdir core\guards
mkdir core\models

# Create assets structure
cd ..\..\..\
mkdir src\assets\images
mkdir src\assets\icons
mkdir src\assets\fonts
mkdir src\assets\styles
mkdir src\assets\styles\themes
mkdir src\assets\styles\components

# Create environments
mkdir src\environments
Step 4: Complete File Structure (What You Should Have)
text
frontend/
├── src/
│   ├── app/
│   │   ├── core/
│   │   │   ├── auth/
│   │   │   │   ├── pages/
│   │   │   │   │   ├── login/
│   │   │   │   │   │   ├── login.component.ts
│   │   │   │   │   │   ├── login.component.html
│   │   │   │   │   │   ├── login.component.scss
│   │   │   │   │   │   └── login.component.spec.ts
│   │   │   │   │   └── register/
│   │   │   │   │       ├── register.component.ts
│   │   │   │   │       ├── register.component.html
│   │   │   │   │       └── register.component.scss
│   │   │   │   ├── services/
│   │   │   │   │   └── auth.service.ts
│   │   │   │   ├── models/
│   │   │   │   │   ├── user.model.ts
│   │   │   │   │   └── auth-response.model.ts
│   │   │   │   ├── guards/
│   │   │   │   │   ├── auth.guard.ts
│   │   │   │   │   └── role.guard.ts
│   │   │   │   ├── interceptors/
│   │   │   │   │   ├── token.interceptor.ts
│   │   │   │   │   └── error.interceptor.ts
│   │   │   │   └── store/
│   │   │   │       ├── auth.actions.ts
│   │   │   │       ├── auth.reducer.ts
│   │   │   │       ├── auth.effects.ts
│   │   │   │       └── auth.selectors.ts
│   │   │   ├── services/
│   │   │   │   ├── api.service.ts
│   │   │   │   └── notification.service.ts
│   │   │   └── core.module.ts
│   │   │
│   │   ├── features/
│   │   │   ├── products/
│   │   │   │   ├── pages/
│   │   │   │   │   ├── product-list/
│   │   │   │   │   │   ├── product-list.component.ts
│   │   │   │   │   │   ├── product-list.component.html
│   │   │   │   │   │   └── product-list.component.scss
│   │   │   │   │   ├── product-detail/
│   │   │   │   │   │   ├── product-detail.component.ts
│   │   │   │   │   │   ├── product-detail.component.html
│   │   │   │   │   │   └── product-detail.component.scss
│   │   │   │   │   └── product-create/
│   │   │   │   │       ├── product-create.component.ts
│   │   │   │   │       ├── product-create.component.html
│   │   │   │   │       └── product-create.component.scss
│   │   │   │   ├── services/
│   │   │   │   │   └── product.service.ts
│   │   │   │   ├── models/
│   │   │   │   │   └── product.model.ts
│   │   │   │   ├── store/
│   │   │   │   │   ├── product.actions.ts
│   │   │   │   │   ├── product.reducer.ts
│   │   │   │   │   ├── product.effects.ts
│   │   │   │   │   └── product.selectors.ts
│   │   │   │   └── products.module.ts
│   │   │   │
│   │   │   ├── orders/
│   │   │   │   ├── pages/
│   │   │   │   │   ├── order-list/
│   │   │   │   │   ├── order-detail/
│   │   │   │   │   └── order-create/
│   │   │   │   ├── services/
│   │   │   │   ├── models/
│   │   │   │   └── orders.module.ts
│   │   │   │
│   │   │   ├── payments/
│   │   │   │   ├── pages/
│   │   │   │   │   └── payment-list/
│   │   │   │   ├── services/
│   │   │   │   ├── models/
│   │   │   │   └── payments.module.ts
│   │   │   │
│   │   │   ├── inventory/
│   │   │   │   ├── pages/
│   │   │   │   │   └── inventory-dashboard/
│   │   │   │   ├── services/
│   │   │   │   ├── models/
│   │   │   │   └── inventory.module.ts
│   │   │   │
│   │   │   └── dashboard/
│   │   │       ├── pages/
│   │   │       │   └── dashboard/
│   │   │       ├── components/
│   │   │       │   ├── sales-chart/
│   │   │       │   ├── recent-orders/
│   │   │       │   └── inventory-status/
│   │   │       ├── services/
│   │   │       └── dashboard.module.ts
│   │   │
│   │   ├── shared/
│   │   │   ├── components/
│   │   │   │   ├── buttons/
│   │   │   │   ├── cards/
│   │   │   │   ├── modals/
│   │   │   │   ├── tables/
│   │   │   │   └── forms/
│   │   │   ├── directives/
│   │   │   ├── pipes/
│   │   │   ├── models/
│   │   │   │   └── api-response.model.ts
│   │   │   ├── constants/
│   │   │   │   └── api.constants.ts
│   │   │   └── shared.module.ts
│   │   │
│   │   ├── app-routing.module.ts
│   │   ├── app.component.ts
│   │   ├── app.component.html
│   │   ├── app.component.scss
│   │   └── app.module.ts
│   │
│   ├── assets/
│   │   ├── images/
│   │   ├── icons/
│   │   ├── fonts/
│   │   └── styles/
│   │       ├── themes/
│   │       ├── components/
│   │       ├── _variables.scss
│   │       ├── _mixins.scss
│   │       └── global.scss
│   │
│   ├── environments/
│   │   ├── environment.ts
│   │   ├── environment.prod.ts
│   │   └── environment.staging.ts
│   │
│   ├── index.html
│   ├── main.ts
│   └── styles.scss
│
├── angular.json
├── package.json
├── tsconfig.json
├── tsconfig.app.json
├── .eslintrc.json
├── .prettierrc
└── README.md
📝 Core Files Content
1. Environment Configuration
src/environments/environment.ts

typescript
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
  authUrl: 'http://localhost:8080/api/auth',
  productsUrl: 'http://localhost:8080/api/products',
  inventoryUrl: 'http://localhost:8080/api/inventory',
  ordersUrl: 'http://localhost:8080/api/orders',
  paymentsUrl: 'http://localhost:8080/api/payments',
  appName: 'Enterprise OMS - Development',
  version: '1.0.0'
};
src/environments/environment.prod.ts

typescript
export const environment = {
  production: true,
  apiUrl: 'https://api.enterprise-oms.com/api',
  authUrl: 'https://api.enterprise-oms.com/api/auth',
  productsUrl: 'https://api.enterprise-oms.com/api/products',
  inventoryUrl: 'https://api.enterprise-oms.com/api/inventory',
  ordersUrl: 'https://api.enterprise-oms.com/api/orders',
  paymentsUrl: 'https://api.enterprise-oms.com/api/payments',
  appName: 'Enterprise OMS - Production',
  version: '1.0.0'
};
2. API Constants
src/app/shared/constants/api.constants.ts

typescript
export const API_CONSTANTS = {
  // Auth endpoints
  AUTH: {
    LOGIN: '/auth/login',
    REGISTER: '/auth/register',
    REFRESH: '/auth/refresh',
    LOGOUT: '/auth/logout'
  },
  // Product endpoints
  PRODUCTS: {
    BASE: '/products',
    BY_SKU: (sku: string) => `/products/sku/${sku}`,
    SEARCH: '/products/search'
  },
  // Order endpoints
  ORDERS: {
    BASE: '/orders',
    BY_CUSTOMER: (email: string) => `/orders/customer/${email}`,
    BY_STATUS: (status: string) => `/orders/status/${status}`,
    CANCEL: (id: string) => `/orders/${id}/cancel`
  },
  // Payment endpoints
  PAYMENTS: {
    BASE: '/payments',
    BY_ORDER: (orderId: string) => `/payments/order/${orderId}`
  },
  // Inventory endpoints
  INVENTORY: {
    BASE: '/inventory',
    CHECK: (sku: string) => `/inventory/${sku}`,
    RESERVE: (sku: string) => `/inventory/${sku}/reserve`
  }
};
3. Models
src/app/shared/models/api-response.model.ts

typescript
export interface ApiResponse<T = any> {
  data?: T;
  message?: string;
  status: number;
  timestamp: string;
  path?: string;
  error?: string;
  validationErrors?: Record<string, string>;
}

export interface Page<T> {
  content: T[];
  totalElements: number;
  totalPages: number;
  size: number;
  number: number;
  first: boolean;
  last: boolean;
  empty: boolean;
}
src/app/core/auth/models/user.model.ts

typescript
export interface User {
  id?: string;
  email: string;
  name?: string;
  role?: UserRole;
  createdAt?: Date;
  updatedAt?: Date;
}

export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
  MANAGER = 'MANAGER'
}

export interface LoginRequest {
  email: string;
  password: string;
}

export interface LoginResponse {
  accessToken: string;
  refreshToken: string;
  email: string;
  message: string;
  success: boolean;
}

export interface RegisterRequest {
  email: string;
  password: string;
  name: string;
  role?: UserRole;
}
4. Auth Service
src/app/core/auth/services/auth.service.ts

typescript
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { BehaviorSubject, Observable, tap } from 'rxjs';
import { environment } from '../../../../environments/environment';
import { LoginRequest, LoginResponse, User, RegisterRequest } from '../models/user.model';
import { ApiResponse } from '../../../shared/models/api-response.model';

@Injectable({
  providedIn: 'root'
})
export class AuthService {
  private readonly AUTH_URL = environment.authUrl;
  private currentUserSubject = new BehaviorSubject<User | null>(null);
  public currentUser$ = this.currentUserSubject.asObservable();
  private tokenKey = 'access_token';
  private refreshTokenKey = 'refresh_token';

  constructor(private http: HttpClient) {
    this.loadStoredUser();
  }

  login(credentials: LoginRequest): Observable<LoginResponse> {
    return this.http.post<LoginResponse>(`${this.AUTH_URL}/login`, credentials)
      .pipe(
        tap(response => {
          if (response.success && response.accessToken) {
            this.storeTokens(response.accessToken, response.refreshToken);
            this.setCurrentUser({ email: response.email });
          }
        })
      );
  }

  register(userData: RegisterRequest): Observable<ApiResponse> {
    return this.http.post<ApiResponse>(`${this.AUTH_URL}/register`, userData);
  }

  logout(): void {
    this.clearTokens();
    this.currentUserSubject.next(null);
  }

  refreshToken(): Observable<LoginResponse> {
    const refreshToken = this.getRefreshToken();
    return this.http.post<LoginResponse>(`${this.AUTH_URL}/refresh`, { refreshToken })
      .pipe(
        tap(response => {
          if (response.accessToken) {
            this.storeTokens(response.accessToken, response.refreshToken);
          }
        })
      );
  }

  getToken(): string | null {
    return localStorage.getItem(this.tokenKey);
  }

  getRefreshToken(): string | null {
    return localStorage.getItem(this.refreshTokenKey);
  }

  isAuthenticated(): boolean {
    const token = this.getToken();
    if (!token) return false;
    // Check if token is expired
    try {
      const payload = JSON.parse(atob(token.split('.')[1]));
      return payload.exp * 1000 > Date.now();
    } catch {
      return false;
    }
  }

  private storeTokens(accessToken: string, refreshToken: string): void {
    localStorage.setItem(this.tokenKey, accessToken);
    localStorage.setItem(this.refreshTokenKey, refreshToken);
  }

  private clearTokens(): void {
    localStorage.removeItem(this.tokenKey);
    localStorage.removeItem(this.refreshTokenKey);
  }

  private setCurrentUser(user: User): void {
    this.currentUserSubject.next(user);
  }

  private loadStoredUser(): void {
    const token = this.getToken();
    if (token && this.isAuthenticated()) {
      try {
        const payload = JSON.parse(atob(token.split('.')[1]));
        this.setCurrentUser({ email: payload.sub });
      } catch {
        this.clearTokens();
      }
    }
  }
}
5. Token Interceptor
src/app/core/auth/interceptors/token.interceptor.ts

typescript
import { Injectable } from '@angular/core';
import { HttpInterceptor, HttpRequest, HttpHandler, HttpEvent, HttpErrorResponse } from '@angular/common/http';
import { Observable, throwError, BehaviorSubject } from 'rxjs';
import { catchError, filter, take, switchMap } from 'rxjs/operators';
import { AuthService } from '../services/auth.service';

@Injectable()
export class TokenInterceptor implements HttpInterceptor {
  private isRefreshing = false;
  private refreshTokenSubject: BehaviorSubject<string | null> = new BehaviorSubject<string | null>(null);

  constructor(private authService: AuthService) {}

  intercept(request: HttpRequest<any>, next: HttpHandler): Observable<HttpEvent<any>> {
    const token = this.authService.getToken();
    let authReq = request;

    if (token && !this.isPublicEndpoint(request.url)) {
      authReq = this.addTokenToRequest(request, token);
    }

    return next.handle(authReq).pipe(
      catchError((error: HttpErrorResponse) => {
        if (error.status === 401 && !this.isPublicEndpoint(request.url)) {
          return this.handle401Error(request, next);
        }
        return throwError(() => error);
      })
    );
  }

  private addTokenToRequest(request: HttpRequest<any>, token: string): HttpRequest<any> {
    return request.clone({
      setHeaders: {
        Authorization: `Bearer ${token}`
      }
    });
  }

  private isPublicEndpoint(url: string): boolean {
    const publicEndpoints = ['/auth/login', '/auth/register', '/auth/refresh'];
    return publicEndpoints.some(endpoint => url.includes(endpoint));
  }

  private handle401Error(request: HttpRequest<any>, next: HttpHandler): Observable<HttpEvent<any>> {
    if (!this.isRefreshing) {
      this.isRefreshing = true;
      this.refreshTokenSubject.next(null);

      return this.authService.refreshToken().pipe(
        switchMap((response: any) => {
          this.isRefreshing = false;
          this.refreshTokenSubject.next(response.accessToken);
          return next.handle(this.addTokenToRequest(request, response.accessToken));
        }),
        catchError((error) => {
          this.isRefreshing = false;
          this.authService.logout();
          return throwError(() => error);
        })
      );
    } else {
      return this.refreshTokenSubject.pipe(
        filter(token => token !== null),
        take(1),
        switchMap(token => {
          return next.handle(this.addTokenToRequest(request, token!));
        })
      );
    }
  }
}
6. Auth Guard
src/app/core/auth/guards/auth.guard.ts

typescript
import { Injectable } from '@angular/core';
import { Router, CanActivate, ActivatedRouteSnapshot, RouterStateSnapshot } from '@angular/router';
import { AuthService } from '../services/auth.service';
import { Observable } from 'rxjs';
import { map, take } from 'rxjs/operators';

@Injectable({
  providedIn: 'root'
})
export class AuthGuard implements CanActivate {
  constructor(private authService: AuthService, private router: Router) {}

  canActivate(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): Observable<boolean> {
    return this.authService.currentUser$.pipe(
      take(1),
      map(user => {
        if (this.authService.isAuthenticated()) {
          return true;
        }
        this.router.navigate(['/auth/login'], { queryParams: { returnUrl: state.url } });
        return false;
      })
    );
  }
}
🚀 Next Steps
1. Git Commit
powershell
cd D:\enterprise-order-management-system

# Add all frontend files
git add frontend/
git commit -m "feat(frontend): initialize Angular project with production structure

- Add Angular 17 project with strict mode
- Create production-grade folder structure
- Add core modules (auth, guards, interceptors)
- Add feature modules (products, orders, payments)
- Add shared components and utilities
- Configure environment files
- Add AuthService with JWT handling
- Add TokenInterceptor for API calls
- Add AuthGuard for route protection
- Install Material Design and NgRx dependencies"

# Push to remote
git push origin feature/frontend-angular-implementation
2. Run Angular Development Server
powershell
cd D:\enterprise-order-management-system\frontend
ng serve --open
3. Next Implementation Steps
Would you like me to provide code for:

Login Component (UI + Logic)

Product List Component (Display products from API)

Order Creation Component (Complete order flow with cart)

Dashboard Component (Analytics and charts)

Which component should I implement next?